import React from 'react'
import Col from 'react-bootstrap/Col'

// import { app } from '../services/firebase'
// import pets from '../data/tiny-pets' 

export default function Albums () {
  // const [albums, setAlbums] = useState(pets[0])
/*
  useEffect(() => {
    db.collection('pets')
    .onSnapshot(snap => {
      const items = []
      snap.forEach(doc => items.push(doc.data()))
      setPets(items)
    })
  }, [db]) */

  return (
    <Col>Albums go here...</Col>
  )
}